# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Environment / secrets
.env
.env.*
!.env.example

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.worktrees/

# Tooling artifacts
.playwright-mcp/

# Reference spreadsheets may contain real patient/report data — never commit
src/types/*.xlsx

# Real ICSR/line-list exports carry patient names and report ids — same rule
# as the spreadsheets above. docs/ONDO (1).xml is one of these.
docs/*.xml

# V8 heap-dump reports (written when a process runs out of memory)
report.*.json
