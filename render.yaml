services:
  - type: web
    name: safety-insight-hub
    runtime: python
    rootDir: .
    buildCommand: pip install -r requirements-server.txt
    startCommand: python -m uvicorn server.main:app --host 0.0.0.0 --port $PORT
